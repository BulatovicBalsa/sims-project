<UserControl x:Class="Hospital.Views.Nurse.PatientAccommodation.PatientAccommodationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hospital.Views.Nurse.PatientAccommodation"
             xmlns:viewModel="clr-namespace:Hospital.ViewModels.Nurse.PatientAccommodation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewModel:PatientAccommodationViewModel/>
    </UserControl.DataContext>

    <StackPanel Margin="200,0,200,0">
        <TextBlock Style="{StaticResource MainTextBlock}">Patient</TextBlock>
        <ComboBox Height="30" ItemsSource="{Binding Patients}"
                  SelectedItem="{Binding SelectedPatient}"/>
        <TextBlock Style="{StaticResource MainTextBlock}">Room</TextBlock>
        <ComboBox Height="30" ItemsSource="{Binding PatientReferrals}"
                  SelectedItem="{Binding SelectedReferral}"
                  DisplayMemberPath="ComboBoxString"/>
        <Button Style="{StaticResource ActionButton}" 
                Content="Accommodate patient"
                Margin="0,20,0,0"
                Command="{Binding UseReferralCommand}"/>
    </StackPanel>
</UserControl>
