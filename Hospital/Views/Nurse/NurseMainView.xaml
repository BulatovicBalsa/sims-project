<Window x:Class="Hospital.Views.Librarian.LibrarianMainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hospital.Views.Librarian"
        xmlns:viewModel="clr-namespace:Hospital.ViewModels.Librarian"
        xmlns:patients="clr-namespace:Hospital.ViewModels.Librarian.Patients"
        xmlns:patientAdmission="clr-namespace:Hospital.ViewModels.Librarian.PatientAdmission"
        xmlns:patientAdmissionViews="clr-namespace:Hospital.Views.Librarian.PatientAdmission"
        xmlns:urgentExaminations="clr-namespace:Hospital.ViewModels.Librarian.UrgentExaminations"
        xmlns:urgentExaminationsViews="clr-namespace:Hospital.Views.Librarian.UrgentExaminations"
        xmlns:referrals="clr-namespace:Hospital.ViewModels.Librarian.Referrals"
        xmlns:referralsViews="clr-namespace:Hospital.Views.Librarian.Referrals"
        xmlns:medicationManagement="clr-namespace:Hospital.ViewModels.Librarian.Medication"
        xmlns:medicationManagementViews="clr-namespace:Hospital.Views.Librarian.Medication"
        xmlns:patientAccommodation="clr-namespace:Hospital.ViewModels.Librarian.PatientAccommodation"
        xmlns:patientAccommodationViews="clr-namespace:Hospital.Views.Librarian.PatientAccommodation"
        xmlns:patientVisiting="clr-namespace:Hospital.ViewModels.Librarian.PatientVisiting"
        xmlns:patientVisitingViews="clr-namespace:Hospital.Views.Librarian.PatientVisiting"
        mc:Ignorable="d"
        Title="LibrarianMainView" Height="510" Width="1000"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True">
    
    <Window.DataContext>
        <viewModel:LibrarianMainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type patients:PatientGridViewModel}">
            <local:PatientGridView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type patientAdmission:PatientAdmissionViewModel}">
            <patientAdmissionViews:PatientAdmissionView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type urgentExaminations:UrgentExaminationsViewModel}">
            <urgentExaminationsViews:UrgentExaminationsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type referrals:PatientReferralsViewModel}">
            <referralsViews:PatientReferralsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type medicationManagement:MedicationManagementViewModel}">
            <medicationManagementViews:MedicationManagementView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type patientAccommodation:PatientAccommodationViewModel}">
            <patientAccommodationViews:PatientAccommodationView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type patientVisiting:PatientVisitingViewModel}">
            <patientVisitingViews:PatientVisitingView/>
        </DataTemplate>
    </Window.Resources>

    <Border CornerRadius="15">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="{StaticResource PrimaryBackground1}" Offset="0"></GradientStop>
                <GradientStop Color="{StaticResource PrimaryBackground2}" Offset="1"></GradientStop>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border CornerRadius="15,0,0,15"
                    Grid.Column="0">
                <Border.Background>
                    <SolidColorBrush Color="#4d000000"/>
                </Border.Background>
                <StackPanel>
                    <TextBlock Height="35"
                               Margin="15,25,0,10"
                               Text="Librarian panel"
                               Foreground="White"
                               FontSize="25"
                               FontWeight="Bold"
                               FontFamily="Montserrat"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Left"
                               Opacity="1"/>

                    <RadioButton Style="{StaticResource MenuButton}"
                                 Command="{Binding ShowPatientsViewCommand}"
                                 IsChecked="True">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Books" Style="{StaticResource MenuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton Style="{StaticResource MenuButton}"
                                 Command="{Binding ShowPatientAdmissionViewCommand}">
                        <StackPanel>
                            <TextBlock Text="Patient admission" Style="{StaticResource MenuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton Style="{StaticResource MenuButton}"
                                 Command="{Binding ShowUrgentExaminationsViewCommand}">
                        <StackPanel>
                            <TextBlock Text="Urgent examinations" Style="{StaticResource MenuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton Style="{StaticResource MenuButton}"
                                 Command="{Binding ShowPatientReferralsViewCommand}">
                        <StackPanel>
                            <TextBlock Text="Patient referrals" Style="{StaticResource MenuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton Style="{StaticResource MenuButton}"
                                 Command="{Binding ShowMedicationManagementViewCommand}">
                        <StackPanel>
                            <TextBlock Text="Medication management" Style="{StaticResource MenuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton Style="{StaticResource MenuButton}"
                                 Command="{Binding ShowCommunicationViewCommand}">
                        <StackPanel>
                            <TextBlock Text="Send message" Style="{StaticResource MenuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton Style="{StaticResource MenuButton}"
                                 Command="{Binding ShowPatientAccommodationViewCommand}">
                        <StackPanel>
                            <TextBlock Text="Patient accommodation" Style="{StaticResource MenuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton Style="{StaticResource MenuButton}"
                                 Command="{Binding ShowPatientVisitingViewCommand}">
                        <StackPanel>
                            <TextBlock Text="Visit patients" Style="{StaticResource MenuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                </StackPanel>
            </Border>

            <Border CornerRadius="0,15,15,0"
                    Grid.Column="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="25"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel x:Name="ControlBar"
                                Grid.Row="0"
                                Orientation="Horizontal"
                                FlowDirection="RightToLeft"
                                Background="Transparent"
                                Margin="0,0,5,0"
                                MouseLeftButtonDown="ControlBar_OnMouseLeftButtonDown"
                                MouseEnter="ControlBar_OnMouseEnter">

                        <Button x:Name="BtnClose" 
                                Style="{StaticResource ControlButton}"
                                Click="BtnClose_OnClick"
                                Content="X"
                                Margin="3,0,3,0"/>

                        <Button x:Name="BtnMaximize"
                                Style="{StaticResource ControlButton}"
                                Click="BtnMaximize_OnClick"
                                Content="O"
                                Margin="3,0,3,0"/>

                        <Button x:Name="BtnMinimize" 
                                Style="{StaticResource ControlButton}"
                                Click="BtnMinimize_OnClick"
                                Content="-"
                                Margin="3,0,3,0"/>

                    </StackPanel>

                    <ContentControl Content="{Binding CurrentChildView}"
                                    Grid.Row="1"
                                    Margin="25"/>
                </Grid>
            </Border>
        </Grid>

    </Border>
</Window>
